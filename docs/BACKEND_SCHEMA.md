# Backend & Database Schema Document - PORTFOLIO-NEXT

## Database Engine Overview
The database is hosted on **Supabase** (PostgreSQL). We access it using the standard Supabase Client and Server SDK libraries. 
A seed SQL script is located at [schema.sql](file:///c:/Users/ssuri/OneDrive/Documents/projects/PORTFOLIO-NEXT/supabase/schema.sql).

---

## Supabase PostgreSQL Table Schemas

### 1. `public.profiles`
Stores user profile information synced from the native `auth.users` authentication table.

```sql
create table public.profiles (
  id uuid references auth.users on delete cascade not null primary key,
  username text unique,
  first_name text,
  last_name text,
  phone text,
  bio text,
  profile_picture text,
  role text default 'USER',
  status text default 'ACTIVE',
  created_at timestamp with time zone default timezone('utc'::text, now()) not null,
  updated_at timestamp with time zone default timezone('utc'::text, now()) not null
);
```

### 2. `public.projects`
Stores information for portfolio projects.

```sql
create table public.projects (
  id bigint generated by default as identity primary key,
  title text not null,
  description text not null,
  technologies text[] not null,
  link text,
  image_url text,
  created_at timestamp with time zone default timezone('utc'::text, now()) not null,
  updated_at timestamp with time zone default timezone('utc'::text, now()) not null
);
```

### 3. `public.experiences`
Stores education and job history timelines.

```sql
create table public.experiences (
  id bigint generated by default as identity primary key,
  date_range text not null,
  title text not null,
  place text not null,
  type text check (type in ('EDUCATION', 'WORK')) not null,
  sort_order integer default 0,
  created_at timestamp with time zone default timezone('utc'::text, now()) not null,
  updated_at timestamp with time zone default timezone('utc'::text, now()) not null
);
```

---

## API Endpoints List

| Endpoint | Method | Input Parameters | Authentication | Purpose |
|:---|:---|:---|:---|:---|
| `/api/auth/confirm`| GET | `token_hash`, `type` | Public | Verification token email check callback. Sets cookies. |
| `/api/` | HEAD | None | Public | Health check ping returning code 200 |

---

## Row Level Security (RLS) Policies

To protect database operations, RLS is enabled:
- **Profiles RLS:** Profiles are readable by anyone. Users can update only their own profile mapping their `auth.uid()`.
- **Projects RLS:** Projects are publicly readable. Mutations (insert, update, delete) are locked to profiles where `role = 'ADMIN'`.
- **Experiences RLS:** Experiences are publicly readable. Mutations are locked to profiles where `role = 'ADMIN'`.
